# Compulsory Task 1
hero = "superman" #Declaring the variable hero as superman
print(hero.replace("","^").upper().strip("^"))# Print out the variable hero all in uppercase letters with the ^ symbol before and after every letter then strip the ^ symbol from the ends of the string.
